name: Frontend CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NEXT_PUBLIC_API_URL: http://localhost:7890
  NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: pk_test_fake
  NEXT_PUBLIC_GA_MEASUREMENT_ID: G-FAKE

jobs:
  lint-typecheck-test-build:
    name: Lint, Typecheck, Test & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint || true

      - name: Run TypeScript type checking
        run: npx tsc --noEmit || true

      - name: Run unit tests with coverage
        run: npm test -- --coverage --ci --passWithNoTests

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/
          retention-days: 14

      - name: Build production bundle
        run: npm run build

  # E2E tests require a running backend (Go API + PostgreSQL + Redis).
  # Enable this job when backend services are available in CI (e.g., via docker-compose).
  # e2e:
  #   name: Playwright E2E Tests
  #   runs-on: ubuntu-latest
  #   needs: lint-typecheck-test-build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: npm
  #     - run: npm ci
  #     - run: npx playwright install --with-deps chromium
  #     - run: npx playwright test
  #       env:
  #         NEXT_PUBLIC_API_URL: http://localhost:7890
  #     - uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: playwright-report
  #         path: playwright-report/
  #         retention-days: 14
